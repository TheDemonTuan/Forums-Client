/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  // swcMinify: true,
  // compiler: {
  //   removeConsole: true,
  // },
};

module.exports = nextConfig;
